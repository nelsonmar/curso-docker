# Resolución

Ejecutar:

```
docker-compose up
```

El comando va a levantar dos instancias de la aplicación password-api y una de nginx, a este último le va a cargar la configuración del archivo `nginx.conf` donde se definen los parámetros necesarios para utilizarse como load balancer.
